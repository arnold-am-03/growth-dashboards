# Copia a .env para desarrollo local (no se versiona).
# Genera SECRET_KEY con: python -c "import os;print(os.urandom(32).hex())"
SECRET_KEY=pega-aqui-una-clave-larga-y-aleatoria

# Opcion A (recomendada): hash de la contraseña
#   python scripts/gen_password_hash.py
APP_PASSWORD_HASH=pega-aqui-el-hash

# Opcion B (alternativa simple): contraseña en texto plano (solo local)
# APP_PASSWORD=mi-clave-de-prueba
